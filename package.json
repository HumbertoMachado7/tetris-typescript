{
  "name": "tetris-typescript", // o el nombre de tu proyecto
  "private": true,
  "version": "0.0.0",
  "type": "module", // Asegúrate de que esto esté si usas import/export de ES Modules
  "scripts": {
    "dev": "vite",                     // <--- Para iniciar el servidor de desarrollo
    "build": "tsc && vite build",      // <--- Para compilar para producción
    "preview": "vite preview",         // <--- Para previsualizar la build de producción localmente
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0" // Opcional, si usas ESLint
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
    // ...otras dependencias que tu proyecto necesite...
  },
  "devDependencies": {
    "@types/react": "^18.2.15",
    "@types/react-dom": "^18.2.7",
    "@typescript-eslint/eslint-plugin": "^6.0.0", // Opcional
    "@typescript-eslint/parser": "^6.0.0",      // Opcional
    "@vitejs/plugin-react": "^4.0.3",        // <--- El plugin de React
    "eslint": "^8.45.0",                       // Opcional
    "eslint-plugin-react-hooks": "^4.6.0",     // Opcional
    "eslint-plugin-react-refresh": "^0.4.3",   // Opcional
    "typescript": "^5.0.2",
    "vite": "^4.4.5"                           // <--- Vite mismo
    // ...otras dependencias de desarrollo...
  }
}
